ovmt_defaults:
   target: server
   provider: "{{ansible_virtualization_type|lower}}"
   media: remote

ovmt: "{{ovmt_defaults|combine(parameters|default({}))}}"

ovmt_dependencies:
  server:
    - open-vm-tools
  desktop:
    - open-vm-tools
    - open-vm-tools-desktop

